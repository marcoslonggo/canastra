# Client Environment Configuration Example
# Copy this file to .env and modify values as needed

# Backend API base URL (Vite format - not currently used due to dynamic config)
VITE_API_BASE=http://localhost:3002

# WebSocket server URL (Vite format - not currently used due to dynamic config)
VITE_SERVER_URL=http://localhost:3002

# WebSocket connection URL (Vite format - not currently used due to dynamic config)
VITE_WS_URL=http://localhost:3002

# Frontend development server port (works in both react-scripts and Vite)
PORT=3004

# Development/Testing Quick Login Passwords (Vite format - not currently used)
# These should match existing user accounts in your database
VITE_TEST_MARCOS_PASSWORD=your_marcos_password_here
VITE_TEST_MICHELE_PASSWORD=your_michele_password_here

# NOTE: This project uses dynamic URL configuration in src/config.ts
# The VITE_ variables above are for future compatibility but not currently used